gradio==6.19.0
librosa==0.11.0
numba==0.65.1
llvmlite==0.47.0
numpy==2.4.4
opencv-python-headless==4.13.0.92
pillow==12.2.0
# Visual-variety embeddings (DINOv2 via scripts/fetch_dinov2.py); CPU inference,
# feature degrades gracefully if absent
onnxruntime==1.27.0
cupy-cuda13x[ctk]==14.1.1
# Optional macOS downbeat backend (BEATSYNC_BEAT_BACKEND=beat_this) — not
# installed by the Windows flow; install manually:
#   .venv/bin/pip install beat-this torch torchaudio
# Optional macOS music-structure backend (section labels + stems; Apple Silicon
# only, auto-enabled when installed) — not part of the Windows flow:
#   .venv/bin/pip install all-in-one-mlx "demucs-mlx[convert]"
